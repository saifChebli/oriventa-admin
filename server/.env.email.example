# ========================================
# EMAIL SYSTEM CONFIGURATION
# ========================================
# Required environment variables for the email messaging system

# SMTP Configuration (for sending emails)
# ----------------------------------------
# SMTP server hostname
SMTP_HOST=pro3.mail.ovh.net

# SMTP server port (465 for SSL, 587 for TLS)
SMTP_PORT=587

# SMTP authentication username (email address)
SMTP_USER=contact@oriventa-pro-service.tn

# SMTP authentication password
SMTP_PASS=''

# IMAP Configuration (for receiving emails)
# ----------------------------------------
# IMAP server hostname
IMAP_HOST=pro3.mail.ovh.net

# IMAP server port (993 for SSL)
IMAP_PORT=993

# IMAP authentication username (usually same as SMTP_USER)
IMAP_USER=no-reply@oriventa-pro-service.tn

# IMAP authentication password (usually same as SMTP_PASS)
IMAP_PASS=''

# Use TLS/SSL for IMAP (true/false)
IMAP_TLS=true

# How often to check for new emails in milliseconds (default: 60000 = 1 minute)
IMAP_FETCH_INTERVAL=60000

# ========================================
# USAGE INSTRUCTIONS
# ========================================

# 1. Copy this file to .env in your server directory
# 2. Replace placeholder values with your actual credentials
# 3. Keep this file secure and never commit it to version control
# 4. Add .env to your .gitignore file

# ========================================
# SECURITY NOTES
# ========================================

# - Never share your email credentials
# - Use strong passwords for email accounts
# - Consider using app-specific passwords if available
# - Regularly rotate your credentials
# - Monitor email server logs for suspicious activity

# ========================================
# TESTING
# ========================================

# To test SMTP connection:
# - The mailService will log connection status on startup
# - Check console for "✅ SMTP Server is ready to send emails"

# To test IMAP connection:
# - The imapService will log connection status when initialized
# - Check console for "✅ IMAP Connection established"

# ========================================
# TROUBLESHOOTING
# ========================================

# If emails are not sending:
# 1. Verify SMTP credentials are correct
# 2. Check if SMTP port is correct (465 for SSL, 587 for TLS)
# 3. Ensure firewall allows outgoing connections to SMTP port
# 4. Check email server logs for authentication failures

# If emails are not being received:
# 1. Verify IMAP credentials are correct
# 2. Check if IMAP port is correct (993 for SSL)
# 3. Ensure inbox has unread messages to fetch
# 4. Check IMAP_FETCH_INTERVAL is reasonable (not too frequent)
# 5. Monitor console logs for IMAP errors

# Common error messages:
# - "Invalid login" - Check username/password
# - "Connection timeout" - Check host and port
# - "SSL/TLS error" - Check IMAP_TLS setting
